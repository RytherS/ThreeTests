export enum MouseButton {
    Left    = 0, // 0 maps to left mouse button of normal MouseEvent
    Right   = 2 // 2 maps to right mouse button of normal MouseEvent
}

export enum ZoomDirection {
    In,
    Out
}